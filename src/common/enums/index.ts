export enum ERole {
  USER = 1,
  ADMIN = 2,
}

export enum EVip {
  TYPE_0 = 1,
  TYPE_1 = 2,
  TYPE_3 = 3,
  TYPE_6 = 4,
}

export enum EReleaseStatus {
  RELEASING = 1,
  RELEASED = 2,
}

export enum ECensorshipStatus {
  PENDING = 1,
  APPROVED = 2,
  REJECTED = 3,
}
